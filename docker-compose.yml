services:

  # ─── PostgreSQL ──────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB:       ${DB_NAME:-gamyam_360_django}
      POSTGRES_USER:     ${DB_USER:-gamyam_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-gamyam_pass}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # ⚠ NO host ports exposed intentionally.
    # Postgres only needs to be reachable inside Docker (backend connects via db:5432).
    # Exposing a host port (e.g. 5433:5432) causes conflicts when multiple projects run together.
    # To inspect the DB directly from host, use:
    #   docker exec -it gamyam360-db-1 psql -U gamyam_user -d gamyam_360_django
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-gamyam_user} -d ${DB_NAME:-gamyam_360_django}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Redis ───────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    # ⚠ NO host ports exposed intentionally.
    # Redis only needs to be reachable inside Docker (backend connects via redis:6379).
    # Exposing a host port (e.g. 6380:6379) causes conflicts when multiple projects run together.
    # To inspect Redis directly from host, use:
    #   docker exec -it gamyam360-redis-1 redis-cli
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  # ─── Django Backend ──────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      target: production
    restart: unless-stopped
    env_file: ./backend/.env.docker
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.production
      DB_HOST:   db
      DB_PORT:   "5432"
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    volumes:
      - media_files:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["sh", "-c", "python manage.py migrate --noinput && python manage.py init_superadmin && gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 120 --access-logfile -"]

  # ─── Celery Worker ───────────────────────────────────────────────────────────
  celery_worker:
    build:
      context: ./backend
      target: production
    restart: unless-stopped
    env_file: ./backend/.env.docker
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.production
      DB_HOST:   db
      DB_PORT:   "5432"
      REDIS_URL: redis://redis:6379/0
    volumes:
      - media_files:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    command: celery -A config worker --loglevel=info --concurrency=2

  # ─── Celery Beat (Scheduler) ─────────────────────────────────────────────────
  celery_beat:
    build:
      context: ./backend
      target: production
    restart: unless-stopped
    env_file: ./backend/.env.docker
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.production
      DB_HOST:   db
      DB_PORT:   "5432"
      REDIS_URL: redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    command: >
      celery -A config beat --loglevel=info
        --scheduler django_celery_beat.schedulers:DatabaseScheduler

  # ─── React Frontend ──────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      args:
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID:-}
        VITE_USE_MOCK: "false"
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  media_files:
